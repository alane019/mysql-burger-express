
  <container id="page-container" class="container"> 
  <header class="hero">
    <h1 class="title burger-title">Eat Da Burger!</h1>
    <figure id="figure1">
    <img id="burgerman" width="40%" src="/assets/images/burgerman.png">
      <figcaption> &nbsp; Figure 1: Walt Burgerman </figcaption>
    </figure>
  </header>

  <main> 
  <div class="columns"> 
    <div class="column is-two-fifths burger-col">
      <h2>Buffet of Hamburgers </h2>
      <ul>
        {{#each burgers}}
          {{#unless devoured}}
            {{> burgers/undevoured-block devoured=false}}
          {{/unless}}
        {{/each}}
      </ul>
    </div>

    <div class="column is-two-fifths burger-col">
      <h2>Burgers, Devoured</h2>
      <ul>
        {{#each burgers}}
          {{#if devoured}}
            {{> burgers/devoured-block devoured=true}}
          {{/if}}
        {{/each}}
      </ul>
    </div>
  </div>
</main>
<footer id="add-burger"> 
   <section id="add-burger">
   <h2>Add a new burger!</h2>
    <form action="/api/burgers/" method="POST"  class="create-form">
      <div class="form-group">
        <label for="new_burger">Burger Name: </label>
        <input id="new_burger" type="text" name="new_burger" class="input" type="text" placeholder="Enter name of new burger">
      </div>
      <button type="submit">Add Burger</button>
    </form>
  </section>
  <section id="other-footer-info"> <div id="other-footer-content"> 2021 &copy; Wow such burgers Ltd </div></span></section>
</footer>
</container>


